/* Optional provider configuration. Open-Meteo is the default and deliberately needs no API key. */
window.FARM_CONFIG = {
  WEATHER_PROVIDER: "open-meteo",
  WEATHER_API_KEY: "" // Add a key here only if you later change to a paid weather provider.
};
